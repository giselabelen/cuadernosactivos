from setuptools import setup

setup(name='seguimiento',
      version='0.1',
      description='POC secuencias de ejercicios con seguimiento de actividad',
      #url=,
      author='Gisela Confalonieri',
      #author_email=,
      #license=,
      packages=['seguimiento'])
      #zip_safe=)